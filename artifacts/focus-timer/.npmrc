# Ignore platform-specific optional dependencies for non-matching platforms
# This prevents EBADPLATFORM errors when deploying from Windows to Linux
# npm will install Linux packages on Linux and skip Windows packages
optional=true
